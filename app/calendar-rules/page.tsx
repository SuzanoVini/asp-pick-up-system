export default function CalendarRulesPage() {
	return (
		<div>
			<h1 className="text-2xl font-bold">Calendar Rules</h1>
			<p className="mt-2 text-gray-500">Configure attendance rules for breaks, holidays, early dismissals, and schedule exceptions.</p>
		</div>
	);
}
