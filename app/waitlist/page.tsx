export default function WaitlistPage() {
	return (
		<div>
			<h1 className="text-2xl font-bold">Waitlist</h1>
			<p className="mt-2 text-gray-500">Track pre-enrollment waitlist entries and manage enrollment offers.</p>
		</div>
	);
}
